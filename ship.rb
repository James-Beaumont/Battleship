class Ship
    def initialize(name, length, symbol)
        @name = name
        @length = length
        @symbol = symbol
    end

    def name
        return @name
    end

    def length
        return @length
    end

    def symbol
        return @symbol
    end
end